{
  "name": "@lloydhumphreys/components",
  "version": "0.1.0",
  "private": true,
  "description": "A shadcn-compatible component registry.",
  "type": "module",
  "scripts": {
    "registry:build": "shadcn build",
    "demo:build": "esbuild registry/scroll-rail/scroll-rail.ts --bundle --format=iife --global-name=ScrollRail --outfile=public/demo/scroll-rail.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "shadcn": "^4.13.0"
  }
}
